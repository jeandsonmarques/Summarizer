# Tests

Development-only checks live in this directory so they do not ship inside the QGIS plugin ZIP.

Current smoke test:

```bash
py -3 tests/test_report_view_smoke.py
```

Plugin opening smoke test for Codex:

```bash
py -3 tests/test_plugin_open_smoke.py
```
